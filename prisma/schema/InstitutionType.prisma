model InstitutionType {
  id           String          @id @default(uuid())
  name         String          @unique 
  institutions Institution[] // Relation to Institution

  @@map("institutionsTypes") // Maps to the "institutionsTypes" table
}

// InstitutionType Table

